doctype html
html(lang="en")
	head
		meta(charset="UTF-8")
		meta(http-equiv="X-UA-Compatible", content="IE=edge")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		link(rel="stylesheet", href="libs/libs.min.css")
		link(rel="stylesheet", href="css/styles-default.css" preload)
		link(rel="stylesheet", href="css/styles-mq.css")
		link(rel="stylesheet", href="css/styles.css")
		title Document

	body
		h1 title3213asd255
	header
		p hu123123

	.swiper
		.swiper-wrapper
			.swiper-slide
				span 1
			.swiper-slide
				span 2
			.swiper-slide
				span 3
			.swiper-slide
				span 4
		.swiper-button-prev
		.swiper-button-next

	include components/components/tabs
	include components/components/accordion
	include components/components/validation
	include components/components/validation2

	p red

	script(src="libs/libs.min.js")
	script(src="js/main.min.js" type="module")
	script.
		const show = document.querySelector("span[data-name='show']");
		const hide = document.querySelector("span[data-name='hide']");
		const showConfirm = document.querySelector("span[data-name='showConfirm']");
		const hideConfirm = document.querySelector("span[data-name='hideConfirm']");

		const togglePassword = () => {
			const psw = document.querySelector("input[data-name='password']");

			if (psw.type === 'password') {
				psw.type = 'text';
				show.style.visibility = 'hidden';
				hide.style.visibility = 'visible';
			} else {
				psw.type = 'password';
				show.style.visibility = 'visible';
				hide.style.visibility = 'hidden';
			}
		}
		const toggleConfirm = () => {
			const confirm = document.querySelector("input[data-name='passwordConfirm']");

			if (confirm.type === 'password') {
				confirm.type = 'text';
				showConfirm.style.visibility = 'hidden';
				hideConfirm.style.visibility = 'visible';
			} else {
				confirm.type = 'password';
				showConfirm.style.visibility = 'visible';
				hideConfirm.style.visibility = 'hidden';
			}
		};
